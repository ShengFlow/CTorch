# 本配置文件有以下书写格式：
# "#"号之后为行注释
# "single:<测试名>"来启用某一个单独测试
# "group:<测试组名>"来启用某一个测试组
# "all"来启用全部测试
# 默认使用全部测试

# test_grad_debug 依赖已删除的 v1 AD API（AutoDiff/AutoDiffContext），
# 暂不开启；待用 v2 AD 重写后再放回测试组。
# single:test_mul_matmul
# single:test_mul_simple
all
