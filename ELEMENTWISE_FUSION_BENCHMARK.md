# 逐元素算子 / 融合 kernel 实测基准笔记 🐰

> 日期:2026-08-31 · 机器:macOS (Apple Silicon, AArch64) · 构建:Release `-O2`
> 一句话主旨:**加速比不是常数,是「分段、非单调、带拐点」的函数**——真实场景里到处都是这种效果,
> 所以任何单一规模拍脑袋的性能结论都不可信。

***

## 1. 为什么做这次实测

论文里把「逐元素算子融合」当作候选卖点之一,但 C3 是融合/二进制编译后端,其逐元素算子理论上靠
`linalg.generic` 声明式统一生成。评估它到底值不值得写进论文、怎么写,必须用**真实机器数据**说话。
于是分两个 bench 实测:

* **单算子**:`linalg.generic`(JIT 编译, C3 后端) vs 手写 C++ 循环 vs 显式分段(VL=8)。
  回答「同一个逐元素算子,C3 的 JIT 实现相对手写是不是慢」。

* **融合链**:把一串逐元素算子融合进单个 kernel(1 趟内存) vs eager 逐算子(多趟往返)。
  回答「逐元素**融合**是快是慢、什么时候快什么时候慢」。

***

## 2. 测试方法(可复现)

### 2.1 入口

| 目的               | bench                         | 源文件                                                    |
| ---------------- | ----------------------------- | ------------------------------------------------------ |
| 单算子 linalg vs 手写 | `bench_linalg_vs_handwritten` | `src/tests/standalone/bench_linalg_vs_handwritten.cpp` |
| 链融合 vs eager     | `bench_fusion_scale`          | `src/tests/standalone/bench_fusion_scale.cpp`          |

### 2.2 构建(关键:必须 Release!)

> **为什么必须 Release:** C3 的 `LinalgElementwiseKernel` 内部用它自己的 LLVM O3 JIT;而 bench 里的
> 手写 scalar/seg 循环是**按 bench 自身的编译优化级别**编译的。若用 Debug(默认 `-O0`)构建,
> 相当于「JIT 是 O3、手写是 O0」,对比数据完全失真(会错误地显得 JIT 大胜)。这曾是本 repo 的一个大坑。

```bash
cmake -S . -B build-release -G Ninja \
      -DCMAKE_BUILD_TYPE=Release -DCT_ENABLE_MLIR=ON -DCT_DISABLE_C3=OFF
ninja -C build-release bench_linalg_vs_handwritten bench_fusion_scale
cd build-release && ./bench_linalg_vs_handwritten && ./bench_fusion_scale
```

### 2.3 前置修复(重要!)

首次链接 `bench_fusion_scale` 失败:报 `mlir::c3::SoftmaxOp::build(...)` 未定义。
根因:[`c3/src/C3/C3Dialect.cpp`](c3/src/C3/C3Dialect.cpp) 里为 Transpose/SumReduce/CrossEntropy/MatMul 手写了
自定义 `build`,但**漏了** **`SoftmaxOp`**。而 `MLIRKernelGen` 生成 softmax 时会调它的 int64 版 `build`,
导致整个 C3 全量 MLIR 路径无法链接。→ 已补上 `SoftmaxOp::build`(与 SumReduce 同款式)。
**这条连线是历史遗留缺口,修复后** **`bench_fusion_scale`** **才能跑。**

### 2.4 指标与扫描范围

* 指标:每元素耗时 `ns/elem`(单算子)或单次链执行耗时 `us`(融合链),**加速比 = eager / c3**(>1 表示 C3 快)。

* 单算子规模扫描:`1024 / 65536 / 1048576 / 4194304`。

* 链融合规模扫描:`1M(4MB) / 4M(16MB) / 16M(64MB) / 32M(128MB) / 64M(256MB)`。

***

## 3. 数据趋势

### 3.1 单算子:linalg(JIT) vs hand-written 手写 (ns/elem, 同 LLVM O3)

| 算子      | n     | linalg    | scalar    | seg(VL=8) |
| ------- | ----- | --------- | --------- | --------- |
| ReLU    | 1024  | 0.497     | **0.046** | 0.129     |
| ReLU    | 65536 | **0.062** | 0.059     | 0.131     |
| ReLU    | 1M    | 0.071     | **0.061** | 0.140     |
| ReLU    | 4M    | 0.089     | **0.088** | 0.151     |
| Sigmoid | 1024  | 1.668     | **1.301** | 1.301     |
| Sigmoid | 65536 | **1.215** | 1.303     | 1.303     |
| Sigmoid | 1M    | **1.228** | 1.329     | 1.329     |
| Sigmoid | 4M    | **1.224** | 1.323     | 1.323     |
| Add     | 1024  | 0.527     | **0.055** | 0.204     |
| Add     | 65536 | **0.090** | 0.101     | 0.240     |
| Add     | 1M    | 0.104     | **0.098** | 0.255     |
| Add     | 4M    | 0.135     | **0.134** | 0.258     |

**趋势:** 大数组(n≥1M)两手基本持平(linalg 与 scalar 差 ≤7%);**小数组(n=1024)linalg 慢 9\~11×**
(ReLU 0.497 vs 0.046, Add 0.527 vs 0.055)。`seg` 显式分段在所有规模都最慢。

### 3.2 逐元素链融合:`relu(neg(sigmoid(x)*tanh(x)))`,融合 1 pass vs eager 5 pass

> 注:这是 5 个逐元素算子(eager 5 趟内存往返)融合成单个 kernel(eager 1 趟)。

| N   | 内存    | eager(us) | c3(us)   | speedup   | 结论        |
| --- | ----- | --------- | -------- | --------- | --------- |
| 1M  | 4MB   | 20554.5   | 4806.2   | **4.28×** | ✅ C3 快    |
| 4M  | 16MB  | 12365.2   | 19290.2  | **0.64×** | ❌ eager 快 |
| 16M | 64MB  | 48378.9   | 76593.9  | **0.63×** | ❌ eager 快 |
| 32M | 128MB | 96116.6   | 153475.5 | **0.63×** | ❌ eager 快 |
| 64M | 256MB | 190983.7  | 306738.7 | **0.62×** | ❌ eager 快 |

**趋势&拐点:** 4MB 时是 **4.28× 的甜点**(省 5 趟 vs 1 趟内存往返碾压);一过 \~4–16MB 内存带宽饱和点,
**直接塌到 0.62–0.64×** 并冻结。**加速比在这里是「↑4.28 → ↓0.64」的悬崖式分段,不是一条平线。**

### 3.3 MatMul+Act 融合:`sigmoid(X@W+B)`

| 形状            | eager(us) | c3(us)  | speedup   | 结论        |
| ------------- | --------- | ------- | --------- | --------- |
| 128×512×512   | 151.5     | 320.6   | **0.47×** | ❌ eager 快 |
| 128×1024×1024 | 546.9     | 376.7   | **1.45×** | ✅ C3 快    |
| 64×2048×2048  | 1462.2    | 1105.0  | **1.32×** | ✅ C3 快    |
| 64×4096×4096  | 6168.1    | 4324.9  | **1.43×** | ✅ C3 快    |
| 32×4096×4096  | 4550.2    | 3486.4  | **1.31×** | ✅ C3 快    |
| 16×8192×8192  | 18377.8   | 13308.3 | **1.38×** | ✅ C3 快    |

**趋势&拐点:** 小 GEMM(512²)是 **0.47×**(融合还倒亏),一到 1024² 就稳定翻正为 **1.32–1.45×**。
同样的非单调、带拐点特性。

***

## 4. 原因分析

三层开销在竞争,谁主导谁就赢:

1. **内存往返(复访成本)** —— 链越长、被反复读写的中间 buffer 越大,融合省得越多。
2. **带宽饱和** —— 当数据量超过内存带宽承受,每次 pass 都是顺序吞吐,「5 pass vs 1 pass」的往返节省占比急剧变小。
3. **计算/超越函数** —— 融合 kernel 在多 op 链里内联的 sigmoid/tanh 等,走的是较慢的实现;而 eager 的手写 SIMD
   倒是针对这些做过调优(前述 SIMD math 有 \~4.24× 领先)。

对照上表:

* **单算子大数组持平**:逐元素是访存密集、计算≈免费;手写 O3 已到带宽上限,融合压无可压。
  而且 JIT 版本有时反而不占优,因为走 `linalg` 的 memref descriptor 结构。

* **单算子小数组 linalg 慢 9\~11×**:固定开销占比 —— `invokePacked` 的 **memref descriptor 打包 + 跨 C-ABI/间接函数调用**,
  几十 ns 的开摊不到小数组上。这是 JIT 逐元素算子在小张量场景的真实劣势。

* **逐元素链 4MB 甜点 vs ≥16MB 悬崖**:小内存时「5 趟→1 趟」省下的复访是真金白银(内存延迟主导、带宽未满)→ +4.28×;
  带宽一满,每次 pass 都是顺序吞吐,融合的读写节省占比骤降,同时**超越函数内联的计算短板暴露为瓶颈** → 塌到 0.63×。

* **MatMul+Act 512² 亏 / 1024² 赚**:GEMM 是计算密集(FLOPs 巨大),内存往返占比小,融合省 add/sigmoid 的
  调度+内存是纯增益,超越函数短板被 GEMM 算力掩盖 → 稳定 +1.3\~1.45×。但在 512²(FLOPs 太少)时,GEMM 本身的
  codegen/调度固定开销占比高,融合反而倒亏 → 0.47×。

***

## 5. 经验教训(写进论文 / 指导后续优化) 🎯

1. **加速比是「分段 / 非单调 / 带拐点」的函数,不是常数。** 任何「单规模测一次」就下的性能结论都不可信;
   必须做规模扫描并标注拐点。这不只是本 repo 的现象,是带权带宽竞争下的普遍规律(真实应用里到处都是)。
2. **逐元素/访存密集的融合,只在「内存未饱和 + 高复访链」时会赢;带宽饱和后必输**,且会暴露超越函数计算速率短板。
   → **不能拿 standalone 逐元素链融合当独立卖点。**
3. **逐元素的正确形态是「焊进计算密集算子(MatMul)」**,这才是稳定 +1.3\~1.45× 的地方 → 论文应以此为重点。
4. **超越函数(sigmoid/tanh)的 SIMD 向量化是逐元素一路的真正堵点**,和「带宽饱和」叠加决定了纯链的负收益。
5. **bench 对比必须 Release 构建**(Debug 下 JIT=O3 vs 手写=O0,数据全假)。
6. 修了一个历史链接 bug:`SoftmaxOp::build` 缺失导致 C3 全量 MLIR 路径无法链接(见 2.3)。

